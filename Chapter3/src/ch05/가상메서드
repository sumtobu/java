프로그램이 로드가 되면
크게 두가지 영역  1.code 영역과 , 2.data 영역(상수, static)


1 : 메서드(인스턴스 마다 다르지 않음 변수가 다르다), 인스트럭션 set
2 : static,리터럴 

함수이름은 주소 같으면 안됨 ,오버로딩의 경우 더미를 붙여 조금씩 다름

지역변수 : stack
인스턴스 : heap

자바는 모든 메서드가 가상함수 디폴트

메서드(함수)의 이름은 주소값을 나타냄
메서드는 명령어의 set이고 프로그램이 로드되면 메서드영역(코드 영역)에 명령어 set이 위치
해당 메서드가 호출되면 명려어 set 이 있는 주소를 찾아가 명령어가 실행됨
이때 메서드에서 사용한는 변수들은 스택 메모리에 위치